apiVersion: v1
kind: Service
metadata:
  name: mcp-azure-personal
  namespace: mcp-azure-personal
spec:
  type: ClusterIP
  selector:
    app: mcp-azure-personal
  ports:
    - name: http
      port: 80
      targetPort: proxy
